# Directory-Cleaner-linux-
Bash script which Cleans the directory(containing a chaos of files with all types of extensions) and puts them into different type of directories with the respective extension name.
